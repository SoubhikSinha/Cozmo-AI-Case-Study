# Device Matrix (Part 1 deliverable)

Generated by `scripts/generate_device_matrix.py` directly from `docs/benchmark_results.json` (produced by `scripts/run_benchmark.py` against the real benchmark set) -- every number below is a real measured result, not a manual claim.

| Tier | Device Tested | Accuracy Achieved (per gate) | Notes |
|---|---|---|---|
| lidar | iPhone17,1 | ceiling_height: FAIL (absolute error (cm) = 5.810, threshold 1.500)<br>opening_widths: FAIL (fraction within 2.0cm = 0.000, threshold 0.850) | -- |
| video | iPhone17,1 | video_tier_wall_lengths: FAIL (worst-wall relative error = 0.438, threshold 0.030) | -- |
| photo | iPhone17,1 | ceiling_repeatability: PASS (spread across captures (cm) = 0.000, threshold 1.000)<br>repeatability_walls: PASS (worst-wall ratio to threshold (<=1.0 passes) = 0.000, threshold 1.000)<br>photo_tier_footprint: PASS (relative footprint error = 0.003, threshold 0.080) | photo tier's ceiling height is a fixed default, not measured -- this comparison is vacuous<br>photo tier's wall lengths are a fixed default, not measured -- this comparison is vacuous<br>this is bedroom_1's single-room footprint, a proxy -- the real gate is the whole-property stitched footprint, not yet run against real multi-room captures |

Only `bedroom_1` (the one room with rectangular ground truth) is represented here -- see `docs/benchmark_report.md` for why the other 3 real rooms aren't auto-scored yet.
